WEBVTT

00:00:00.000 --> 00:00:43.560
UNIVERSITY OF INFORMATION AND COMMUNICATION TECHNOLOGY
FACULTY OF INFORMATION TECHNOLOGY
Lesson #8:
CONTINUOUS DELIVERY &
DEPLOYMENT (CD)
THAI NGUYEN, MONTH … YEAR 2025

Lesson objectives
1Understand the CD process
Master the continuous delivery (CD pipeline) and continuous
deployment processes. 2Package applications with Docker
Know how to package applications as images with Docker. 3Apply CD with Jenkins
Combine Jenkins with Docker to build an effective CD process.

00:00:43.560 --> 00:01:31.320
University of Information and Communication
Technology

Main content
1Continuous delivery & deployment process - CD
2Package applications with Docker
CD practice with Jenkins
University of Information and Communication
TechnologyUsing Docker with Command Line Tools & Docker Compose3
4

1. Continuous Delivery (CD) Process
❑Continuous Delivery Process = CI +
deploy application to deployment
environments, but deploying to the end -
user environment (Prod) is still manual. University of Information and Communication
Technology

1.

00:01:31.320 --> 00:01:49.090
Continuous Deployment Process
Continuous Deployment = CI
+ CD, but automates
deployment to the end -user
environment (Prod). University of Information and Communication
Technology

2.

00:01:49.090 --> 00:02:29.480
Package applications with Docker
a)Introduction
b)Register & Explore Docker Hub
c)Install Docker Desktop on Windows
d)Check installation & view Docker commands
e)Key elements of Docker
f)Main/core Docker directives
g)Create Dockerfile with basic directives
h)Building a Docker image from Dockerfile
i)Initialize a new Container of the image
j)Push the built Image to Docker Hub
k)Pull the Image from Docker Hub to run a test
University of Information and Communication
Technology

a.

00:02:29.480 --> 00:03:07.290
Docker – Introduction
❑In recent years, a particular technology has attracted attention
in the market, on social networks and at events, namely Docker. ❑Docker is an open source containerization tool from 2013,
which allows isolating applications from the server system. ▪Helps applications become mobile, can be tested on the developer's
workstation and deployed to the production environment without
worrying about runtime dependencies.

00:03:07.290 --> 00:03:37.010
▪It is a cross -platform tool, can be installed on Windows, Linux,
macOS, ...and is available by default on some cloud providers such as
AWS, Azure, ... University of Information and Communication
Technology

Container
A system that embeds the application with its dependencies, containing only a lightweight operating system and
the necessary elements.

00:03:37.010 --> 00:04:19.410
Comparison with VM
Unlike virtual machines (VMs) that contain a full operating system, containers rely on the guest operating system
(the container only contains a lightweight operating system (OS) and the elements necessary for that operating
system such as system libraries, binary files and code dependencies). Portability
Docker makes applications portable by building images that are easy to deploy across different environments
without compatibility issues. University of Information and Communication
Technologya.

00:04:19.410 --> 00:04:55.610
Docker – Introduction

❑To operate,  Docker  needs  the following  elements :
▪Docker  Desktop  UI: manage  containers
▪Docker  client : Is a command  line tool (docker)  that you
use to give commands,  for example : docker  run, docker
build,  ... ▪Docker  daemon : Is the "engine"  of Docker,  responsible
for: Managing  containers,  images,  networks,  volumes ;
Executing  commands  from  the Docker  Client .

00:04:55.610 --> 00:05:24.140
▪Docker  Registry : This is a public  repository  (Docker  Hub)
or a private  repository  of Docker  images  (account
registration  required)
In Docker Desktop, Docker Client and Docker Daemon are
available → run directly on Desktop (Windows /Mac/Linux). Docker Desktop only connects to Docker Hub by default
a. Docker – Introduction

2.

00:05:24.140 --> 00:05:32.970
Package applications with Docker a)Introduction b)Register & Explore Docker Hub c)Install

00:05:32.970 --> 00:05:41.090
Docker Desktop on Windows d)Check installation & view Docker commands e)Key elements of

00:05:41.090 --> 00:05:49.150
Docker f)Main/core directives of Docker g)Create Dockerfile with basic directives

00:05:49.150 --> 00:05:56.920
h)Building a Docker image from Dockerfile i)Initialize a new Container from the image

00:05:56.920 --> 00:06:04.400
j)Push the built Image to Docker Hub k)Pull the Image from Docker Hub to test run

00:06:04.400 --> 00:06:11.440
University of Information Technology and Communications  Docker Hub is a public space

00:06:11.440 --> 00:06:20.000
(registry) containing over 2 million public Docker images from companies, communities, and

00:06:20.000 --> 00:07:09.050
To register, visit https://hub.docker.com/, fill in the
information and click "Sign Up". This account allows us to
upload arbitrary images and download Docker Desktop. After logging in, we can explore the available images in
the "Explore" section, with filters to search for official
images, from verified publishers, or certified by Docker
(Screenshot – Slide below)
University of Information Technology and
Communications
Registered with account ptthuong@ictu.edu.vnb. Register & Explore Docker Hub

b.

00:07:09.050 --> 00:07:30.140
Register & Explore Docker Hub
University of Information Technology and
Communications

b. Register & Explore Docker Hub
University of Information Technology and
Communications

b. Register & Explore Docker Hub

2.

00:07:30.140 --> 00:07:38.970
Package applications with Docker a)Introduction b)Register & Explore Docker Hub c)Install

00:07:38.970 --> 00:07:47.090
Docker Desktop on Windows d)Check installation & view Docker commands e)Key elements of

00:07:47.090 --> 00:07:55.150
Docker f)Main/core directives of Docker g)Create Dockerfile with basic directives

00:07:55.150 --> 00:08:02.920
h)Building a Docker image from Dockerfile i)Initialize a new Container from the image

00:08:02.920 --> 00:08:10.400
j)Push the built Image to Docker Hub k)Pull the Image from Docker Hub to test run

00:08:10.400 --> 00:08:17.380
University of Information Technology and Communications  ❑Docker Desktop is a cross

00:08:17.380 --> 00:08:22.690
-platform tool, free for developers and small teams.

00:08:22.690 --> 00:09:18.080
To install on Windows, we need to ensure:
•Windows 10/11 64 -bit with at least 4GB RAM. •Windows Subsystem for Linux 2 (WSL 2) backend or Hyper -V is enabled. ❑Installation:
(1) Download the installation file from the Docker Desktop for Windows page, then run the .EXE
file and configure the components for the WSL 2 backend. After the installation is complete; (2)
Launch Docker Desktop and log in to Docker Hub. => Specifically as follows:
University of Information Technology and
Communicationsc. Install Docker Desktop on Windows

c.

00:09:18.080 --> 00:10:06.780
Install Docker Desktop on Windows
(1) Download  Docker  Desktop  by
clicking  on the Docker  Desktop  for
Windows  button  on the page :
https ://docs .docker .com/desktop/wind
ows/install/
▪Screenshot :

c. Install Docker Desktop on Windows
❑Installation  & configuration :
Click  on the downloaded
executable  file (.EXE)  to install,
during  the installation  we need  to
install  the components  for the
WSL  2 backend,  by checking  the
box (as shown  in screen  1):
Successful  installation  result :
screen  2

c.

00:10:06.780 --> 00:10:32.270
Install Docker Desktop on Windows
(2) Launch  Docker  Desktop,  register  & log in
to Docker  Hub. ❑Startup  Docker  Desktop  as shown  in the
screenshot :
❑Register  & Login  to Docker  Hub (at the Docker
Desktop  screen)
=> Result : screenshot  (slide  below)

2.

00:10:32.270 --> 00:11:11.980
Package applications with Docker
a)Introduction
b)Register & Explore Docker Hub
c)Install Docker Desktop on Windows
d)Check installation & view Docker commands
e)Key elements of Docker
f)Main/core directives of Docker
g)Create Dockerfile with basic directives
h)Building a Docker image from Dockerfile
i)Initialize a new Container from the image
j)Push the built Image to Docker Hub
k)Pull the Image from Docker Hub to test run
University of Information Technology and
Communications

d.

00:11:11.980 --> 00:11:32.840
Check installation
& view Docker
commands
❑To see the Docker commands,
open DOS, or PowerShell and
execute the command:
 docker --help
❑To check the installed Docker
version, run the command:
docker --version
University of Information Technology and
Communications

2.

00:11:32.840 --> 00:12:12.710
Packaging applications with Docker
a)Introduction
b)Register & Explore Docker Hub
c)Install Docker Desktop on Windows
d)Check installation & view Docker commands
e)Key elements of Docker
f)Main/core directives of Docker
g)Create Dockerfile with basic directives
h)Building a Docker image from Dockerfile
i)Initialize a new Container from the image
j)Push the built Image to Docker Hub
k)Pull the Image from Docker Hub to test run
University of Information Technology and
Communications

1.

00:12:12.710 --> 00:13:02.600
Docker Image
A basic element of Docker, created
from a Dockerfile, containing the
binaries and application files to be
packaged.2. Docker Registry
A centralized storage system for
shared Docker images, which can be
public (Docker Hub) or private (ACR).3. Container
An instance executed from a Docker
image, where the application will run. There can be multiple instances from
the same image. 4. Volume
Physical storage space on the host OS, outside the container,
that can be shared between multiple containers to store data.5.

00:13:02.600 --> 00:13:34.400
Dockerfile
A basic element of Docker, containing instructions for building
a Docker image. University of Information Technology and
CommunicationsBefore learning Docker commands, we need to familiarize ourselves with the concepts:
Docker images, Dockerfiles, containers, and volumes, … to understand the elements of
Dockere. Key elements of Docker

2.

00:13:34.400 --> 00:14:14.280
Packaging applications with Docker
a)Introduction
b)Register & Explore Docker Hub
c)Install Docker Desktop on Windows
d)Check installation & view Docker commands
e)Key elements of Docker
f)Main/core directives of Docker
g)Create Dockerfile with basic directives
h)Building a Docker image from Dockerfile
i)Initialize a new Container from the image
j)Push the built Image to Docker Hub
k)Pull the Image from Docker Hub to test run
University of Information Technology and
Communications

f.

00:14:14.280 --> 00:14:56.590
Main/core directives of Docker
University of Information Technology and
Communications1.FROM  directive :
▪Used  to specify  the base  image  that  the container  will inherit . ▪Each  Dockerfile  must  start  with  at least  one  FROM . ▪Example : FROM  openjdk :11 => Use  the openjdk :11 image  as a base  to build  a
container  to run the Java  application . 2.COPY  directive :
▪Used  to copy  files/folders  from  the host  machine  (where  you  write  the Dockerfile)
into  the container .

00:14:56.590 --> 00:15:22.460
▪Example : COPY  target/myapp .war  /usr/local/tomcat/webapps/  => Copy  the
myapp .war  file from  the target  folder  on the host  machine  to the webapps  folder  in
the Tomcat  container . f. Main/core directives of Docker
University of Information Technology and
Communications3.

00:15:22.460 --> 00:16:07.670
ADD  directive :
▪Similar  to COPY  but more  powerful  (Can  automatically  extract  .tar files,  Supports  URLs  to
download  files from  the Internet)
▪Example : ADD  https ://example .com/app .tar.gz /app/  => Download  the app .tar.gz file
from  the Internet,  copy  it to /app/  in the container  and  automatically  extract  it. 4. RUN  directive :
▪Used  to run commands  during  the image  build  process,  usually  to install  packages,  set up
the environment .

00:16:07.670 --> 00:16:50.650
▪Example : RUN  apt-get update  && apt-get install  -y curl  => Update  the package  list
and  install  the curl  tool  into  the image . f. Main/core directives of Docker
University of Information Technology and
Communications5. ENV  directive :
▪Used  to declare  environment  variables  in the container . ▪Example : ENV  APP_HOME=/usr/local/myapp  => Create  the APP_HOME  environment
variable . Then  it can  be used  in the Dockerfile  or when  the container  is running
6.

00:16:50.650 --> 00:17:36.020
WORKDIR  directive :
▪Sets the default  working  directory  in the container . Subsequent  commands  (RUN,  CMD,
COPY ,  …) will be executed  in this directory . ▪Example : dockerfile :
WORKDIR  /app
COPY  . . RUN  javac  Main .java
CMD  ["java" ,  "Main"]Meaning:
▪Set the default working directory to /app. ▪Copy all source code to /app. ▪Compile the Main.java file. ▪When the container runs, execute java
Main. 2.

00:17:36.020 --> 00:18:15.910
Packaging Applications with Docker
a)Introduction
b)Register & Explore Docker Hub
c)Install Docker Desktop on Windows
d)Check Installation & View Docker Commands
e)Key Elements of Docker
f)Main/Core Directives of Docker
g)Create Dockerfile with Basic Directives
h)Building a Docker image from Dockerfile
i)Initialize a New Container from the Image
j)Push the Built Image to Docker Hub
k)Pull the Image from Docker Hub to Test Run
University of Information Technology and
Communications

g.

00:18:15.910 --> 00:19:00.620
Create Dockerfile with Basic Directives
❑Example of building a Docker image (containing an Apache web server and a web application)
1.Create a simple web application:
Create a folder named appdocker, containing the index.html file, for example with the following content:
2. Then, in the same folder, create a Dockerfile (without extension) with the content (slide below):<html>
<body>
<h1>Welcome to my new app</h1>
This page is test for my demo Dockerfile.<br />
Enjoy ...

00:19:00.620 --> 00:19:28.430
</body>
</html>
University of Information Technology and
Communications

g. Create Dockerfile with Basic Directives
2) Dockerfile content:
Where:
oFROM Directive: specifies the base image that the container will inherit => httpd (latest version) is the
base image in this example.

00:19:28.430 --> 00:20:20.890
=> httpd is the official base image of Apache HTTP Server (the most popular web server in the world) =>
helps to quickly launch an Apache web server in a container (used to deploy static web applications
(HTML, CSS, JS))
oCOPY Directive: executes the image building process. Docker will copy the local index.html file created
into the /usr/local/apache2/htdocs/ directory of the image. University of Information Technology and
CommunicationsFROM httpd:latest
COPY index.html /usr/local/apache2/htdocs/

2.

00:20:20.890 --> 00:20:28.440
Packaging Applications with Docker a)Introduction b)Register & Explore Docker Hub

00:20:28.440 --> 00:20:37.230
c)Install Docker Desktop on Windows d)Check Installation & View Docker Commands e)Key

00:20:37.230 --> 00:20:45.320
Elements of Docker f)Main/Core Directives of Docker g)Create Dockerfile with Basic

00:20:45.320 --> 00:20:53.330
Directives h)Building a Docker image from Dockerfile i)Initialize a New Container from the

00:20:53.330 --> 00:21:01.200
Image j)Push the Built Image to Docker Hub k)Pull the Image from Docker Hub to Test Run

00:21:01.200 --> 00:21:08.560
University of Information Technology and Communications  ❑Open  a terminal  (PowerShell)

00:21:08.560 --> 00:21:15.040
and  navigate  to the directory  containing  the Dockerfile,  then  execute  the docker

00:21:15.040 --> 00:21:20.490
build  command  as follows : docker  build  -t demobook :v1 .

00:21:20.490 --> 00:21:53.710
▪Where :
✓ The  -t argument  specifies  the name  of the image  and  its tag. In this  example,  the name  of the
image  is demobook  and  its tag is v1. ✓ The  . (dot)  at the end  of the command  specifies  that  we will use  the files  in the current  directory . => Screenshot  of command  execution :h. Building a Docker image from Dockerfile
~ package application using Docker

h.

00:21:53.710 --> 00:22:48.020
Building a Docker image from Dockerfile
❑From the running screen, we see that building a Docker image consists of
3 steps:
1)Docker downloads the specified base image (from Docker Hub) (1)
2)Docker copies the index.html file into the image (2)
3)Docker creates and tags the image (3)
Note :
▪If during the execution of the docker build command you encounter the message:
https://registry -1.docker.io/v2/library/httpd/manifests/latest: unauthorized:
incorrect username or password error, try executing the docker logout command.

00:22:48.020 --> 00:23:07.440
Then, re -execute the docker build command. h. Building a Docker image from Dockerfile
❑To check if the image has been created successfully, execute the
command:
docker images
=> Screenshot: created successfully

h.

00:23:07.440 --> 00:23:14.450
Building a Docker image from Dockerfile Note: ▪To build a Docker image from a .jar file

00:23:14.450 --> 00:23:21.750
(Spring Boot or Java app), the Dockerfile will be as follows: ✓Write Dockerfile

00:23:21.750 --> 00:23:29.880
(screenshot) ✓After having the Dockerfile, run the image build command: docker build -t

00:23:29.880 --> 00:23:42.040
myapp:1.0 Explanation: ✓ docker build: image build command ✓ -t myapp:1.0: attach a tag to

00:23:42.040 --> 00:23:52.800
the image (name is myapp, version 1.0) ✓ .: dot (.) ~ build context, meaning Docker takes

00:23:52.800 --> 00:24:00.960
the Dockerfile in the current directory# Use JDK runtime (only need JRE if not building in

00:24:00.960 --> 00:24:08.940
the container) FROM openjdk:17 -jdk-slim # Set the working directory in the container

00:24:08.940 --> 00:24:17.010
WORKDIR /app # Copy the jar file from the host machine to the container COPY myapp.jar

00:24:17.010 --> 00:24:26.760
app.jar # Open the application port (e.g., 8080) EXPOSE 8080 # Command to run when the

00:24:26.760 --> 00:24:32.670
container starts ENTRYPOINT ["java", " -jar", "app.jar"]  2.

00:24:32.670 --> 00:24:40.300
Packaging the application with Docker a)Introduction b)Register & Explore Docker Hub

00:24:40.300 --> 00:24:49.090
c)Install Docker Desktop on Windows d)Check installation & view Docker commands e)Key

00:24:49.090 --> 00:24:57.180
elements of Docker f)Main/core directives of Docker g)Create Dockerfile with basic

00:24:57.180 --> 00:25:05.070
directives h)Building a Docker image from Dockerfile i)Initialize a new Container of the

00:25:05.070 --> 00:25:12.940
image j)Push the built Image to Docker Hub k)Pull the Image from Docker Hub to test run

00:25:12.940 --> 00:25:19.830
University of Information Technology and Communications  ❑After building the Docker image

00:25:19.830 --> 00:25:27.910
for the application, we initialize a new container to test run the application, execute

00:25:27.910 --> 00:25:37.260
the command: docker run -d --name demoapp -p 8080:80 demobook:v1 oWhere: ✓-d: ~ container

00:25:37.260 --> 00:25:45.680
will be in background mode ✓--name: set container name, ✓-p port mapping, port 80 of the

00:25:45.680 --> 00:25:55.260
container will be mapped to port 8080 on the host (our PC) ✓demobook:v1: name of the image

00:25:55.260 --> 00:26:01.180
and its tag University of Information Technology and Communicationsi.

00:26:01.180 --> 00:26:41.000
Initialize a new Container of the image

❑Check the list of running containers using docker ps
University of Information Technology and
Communicationsi. Launch a new container of the image
=> Everything in the container is isolated,
but thanks to port mapping, we can
access the application via the browser at
http://localhost:8080 => Result: The
content of the index.html page is
displayed. Press refresh to test again after stopping the application container. 2.

00:26:41.000 --> 00:27:22.000
Packaging the application with Docker
a)Introduction
b)Register & Explore Docker Hub
c)Install Docker Desktop on Windows
d)Check installation & view Docker commands
e)Key elements of Docker
f)Main/core directives of Docker
g)Create Dockerfile with basic directives
h)Building a Docker image from Dockerfile
i)Initialize a new Container of the image
j)Push the built Image to Docker Hub
k)Pull the Image from Docker Hub to test run
University of Information Technology and
Communications

j.

00:27:22.000 --> 00:28:07.310
Push Image to Docker Hub
❑To share and use Docker images on other computers, we need to push it to a Docker image registry. Docker Hub is a public, free registry. ▪We have registered for Docker Hub (according to the instructions in the previous slides)
=> Screenshot:
University of Information Technology and
Communications

j. Push Image to Docker Hub
1. Log in to Docker Hub
Use the command docker login -u <your dockerhub login> and enter your password. 2. Get Image ID
Run the docker images command to display a list of images and their IDs. 3.

00:28:07.310 --> 00:28:48.320
Tag Image for Docker Hub
Use the image ID to tag: docker tag <image ID> <dockerhub login>/demobook:v1
4. Push Image to Docker Hub
Execute the command docker push docker.io/<dockerhub login>/demobook:v1 => The image
will be uploaded to Docker Hub and can be viewed on the web portal. University of Information Technology and
Communications=> See the push results in the screenshot (slide below)

❑By default,  the image  pushed  to Docker  Hub
is in public  mode  – everyone  can  view  it in the
explorer  and  use  it.

00:28:48.320 --> 00:29:00.830
=> It is possible  to create  a private  image  – only  when
we confirm  can the image  be used  (refer  to textbook
[1])j. Push Image to Docker Hub

2.

00:29:00.830 --> 00:29:41.770
Packaging the application with Docker
a)Introduction
b)Register & Explore Docker Hub
c)Install Docker Desktop on Windows
d)Check installation & view Docker commands
e)Key elements of Docker
f)Main/core directives of Docker
g)Create Dockerfile with basic directives
h)Building a Docker image from Dockerfile
i)Initialize a new Container of the image
j)Push the built Image to Docker Hub
k)Pull the Image from Docker Hub to test run
University of Information Technology and
Communications

k.

00:29:41.770 --> 00:30:28.890
Pull the Image from Docker Hub to test run
Stop the running demobook:v1 container (previously), pull the image from Docker Hub, & test
run
1. View the list of running containers: use the docker ps command
2. Stop the container: run the docker stop <Container ID> command
3. Pull the Image from Docker Hub: run the docker pull phamthuongdocker/demobook:v1 command
4. Run the container from the image
Execute the command docker run -d -p 8080:80
phamthuongdocker/demobook:v1
University of Information Technology and
Communications5.

00:30:28.890 --> 00:30:40.140
Check the application corresponding to the running container
Access: http://localhost:8080Done! 2.

00:30:40.140 --> 00:31:21.090
Package the application with Docker
a)Introduction
b)Register & Explore Docker Hub
c)Install Docker Desktop on Windows
d)Check installation & view Docker commands
e)Key elements of Docker
f)Main/core directives of Docker
g)Create Dockerfile with basic directives
h)Building a Docker image from Dockerfile
i)Initialize a new Container of the image
j)Push the built Image to Docker Hub
k)Pull the Image from Docker Hub to test run
University of Information Technology and
Communications

3.

00:31:21.090 --> 00:31:55.130
Docker Compose
❑Modern applications rarely operate independently, often
requiring dependent services such as databases or APIs. ▪Docker Compose allows defining and managing multiple
containers in the same application. ▪Docker Compose uses a YAML format configuration file to
describe the services, volumes, and networks that make up
the application
oThat is, instead of running each complex docker run ...

00:31:55.130 --> 00:32:33.520
command, we only need to write a docker -compose.yml
file describing the services (applications), then use the
command: docker compose up
o→ Docker will automatically build, create a network, and
launch all containers. Thus: Docker Compose = Managing multi -container applications
(Microservices, Database + Backend + Frontend). University of Information Technology and
Communications

3.

00:32:33.520 --> 00:33:05.020
Docker Compose
❑Docker Compose is available in Docker Desktop, we can check with the command:
docker compose version
University of Information Technology and
Communications

3. Docker Compose – Usage Process
University of Information Technology and
Communications1. Create a docker -compose.yml file to define the services. 2.

00:33:05.020 --> 00:33:48.250
Execute Docker compose to start the containers described in the YAML configuration file,
run the docker -compose up -d command in the directory containing the file
▪ The –d option runs the containers in detached mode, background mode. ▪ You can verify the running containers with the docker ps command, or through Docker Desktop
2. Container lifecycle management:
▪Stop: docker compose down
▪View log: docker compose logs –f
▪Restart: docker compose restart

University of Information Technology and
Communications3.

00:33:48.250 --> 00:34:33.370
Docker Compose – Student Practice
Docker Compose for Java web application + MySQL + Tomcat. ❑Practice, discussion:
▪Suppose we have a Java web application (Maven packages to myapp.war file) and
want to run it on Tomcat. The application needs to connect to the MySQL database. ➢So we will have 2 services in Docker Compose:
✓tomcat → contains Java web application (deploy .war). ✓mysql → database backend. ➢What is the content of the docker -compose.yml file????

00:34:33.370 --> 00:35:14.550
Summary & Reinforcement Questions
This lesson has covered the basic concepts of Docker, from creating Dockerfiles and
images to deploying containers locally and to Docker Hub. We've also explored how to
use Docker Compose to manage multiple containers. Key Concepts
•Docker Hub and Docker Desktop
•Dockerfile and Directives
•docker build, run, push commands
•Docker ComposeReinforcement Questions
•What is Docker Hub? •Which directive defines the base image in a Dockerfile? •Which command creates a Docker image?

00:35:14.550 --> 00:35:47.930
•Which command publishes an image to Docker Hub? •How does CD differ from continuous deployment? University of Information and Communication
Technology

University of Information and Communication
TechnologyPractice, discussion
JOB #3 & JOB #4 PRACTICE:
SEE HERE

UNIVERSITY OF INFORMATION AND COMMUNICATION TECHNOLOGY
FACULTY OF INFORMATION TECHNOLOGY
THAI NGUYEN, MONTH … YEAR 2025Thank you!
